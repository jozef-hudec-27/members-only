<%= form_with model: comment do |form| %>
    <%= form.text_area :body, placeholder: 'Comment must be between 1 and 200 characters', class: 'new-comment-textarea' %>

    <br /><br />

    <%= form.hidden_field :post_id, value: post.id %>

    <%= form.submit %>
<% end %>